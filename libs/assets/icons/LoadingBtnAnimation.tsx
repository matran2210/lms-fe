import React from "react";
// import loadingBtnAnimationIcon from 'public/animations/LoadingBtn.json' comment monorepo
import Lottie from "lottie-react";
import clsx from "clsx";
interface IProps {
  className?: string;
}
const LoadingBtnAnimation = ({ className }: IProps) => {
  return (
    // <Lottie
    //   animationData={loadingBtnAnimationIcon}
    //   loop
    //   autoplay
    //   className={clsx("before-icon inline-block size-5", className)}
    // />
    <></>
  );
};

export default LoadingBtnAnimation;
