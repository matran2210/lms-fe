version: "3.9"

services:
  lms-pro:
    # build:
    #   context: .
    #   dockerfile: ./Dockerfile
    #   args:
    #     APP_NAME: "lms-pro"
    image: gitlab.sapp.edu.vn:5050/lms-pro/lms-fe/lms-fe-next
    container_name: lms-pro
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: always

  finhub:
    # build:
    #   context: .
    #   dockerfile: ./Dockerfile
    #   args:
    #     APP_NAME: "finhub"
    image: gitlab.sapp.edu.vn:5050/frontend/lms-fe/finhub
    container_name: finhub
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: production
    restart: always
 